import DS from 'ember-data';

/**
@module ember-flexberry-offline
**/

/**
 * Adapter that can switch between online and offline modes.
 *
 * @class BaseAdapter
 * @extends DS.Adapter
 * @public
 */
export default DS.Adapter.extend({
});
